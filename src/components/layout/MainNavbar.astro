---
import { logoMain, logoMainPng } from '@/data/assets';
import { getBookingLocalizedPath, getLanguageSwitchLinks, getLocalizedPath, getPathContext, getPlannerLocalizedPath, isPlannerSlug } from '@/utils/localizedLinks';
import { getTranslations } from '@/i18n/config';
import Icon from '@/components/ui/Icon.astro';

const pathContext = getPathContext(Astro.url.pathname);
const t = getTranslations(pathContext.languageCode);
const plannerLabels = {
  pl: 'Planer',
  en: 'Planner',
  de: 'Planer',
  it: 'Planner',
};
const navItems = [
  { slug: '', label: t.nav.items.start, icon: 'home' },
  { slug: 'camping', label: t.nav.items.camping, icon: 'tent' },
  { slug: 'domki', label: t.nav.items.domki, icon: 'house' },
  { slug: 'cennik', label: t.nav.items.cennik, icon: 'pricing' },
  { slug: 'dojazd', label: t.nav.items.dojazd, icon: 'tram' },
  { slug: 'atrakcje', label: t.nav.items.atrakcje, icon: 'landmark' },
  { slug: 'galeria', label: t.nav.items.galeria, icon: 'gallery' },
  { slug: '__planner__', label: plannerLabels[pathContext.languageCode as keyof typeof plannerLabels] ?? plannerLabels.en, icon: 'planner' },
  { slug: 'kontakt', label: t.nav.items.kontakt, icon: 'phone' },
];

const currentLanguage = pathContext.currentLanguage;
const currentSlug = pathContext.slug;
const activeLanguagePrefix = pathContext.explicitLanguage;

const homeHref = getLocalizedPath(activeLanguagePrefix, '');
const bookingHref = getBookingLocalizedPath(pathContext.languageCode);
const localizedNavItems = navItems.map((item) => ({
  ...item,
  href: item.slug === '__planner__' ? getPlannerLocalizedPath(pathContext.languageCode) : getLocalizedPath(activeLanguagePrefix, item.slug),
  active: item.slug === '__planner__' ? isPlannerSlug(currentSlug) : item.slug === currentSlug,
}));
const languageLinks = getLanguageSwitchLinks(Astro.url.pathname);
const navMessages = {
  openMenu: t.nav.openMenu,
  closeMenu: t.nav.closeMenu,
  enableLight: t.nav.enableLight,
  enableDark: t.nav.enableDark,
  lightMode: t.nav.lightMode,
  darkMode: t.nav.darkMode,
};
---

<header class="main-navbar-shell" id="mainNavbarShell">
  <nav class="main-navbar" id="mainNavbar" aria-label={t.nav.aria}>
    <a href={homeHref} class="main-navbar__brand" aria-label={t.nav.homeAria}>
      <img class="main-navbar__logo" src={logoMain} alt="Camping Clepardia" width="210" height="52" onerror={`this.onerror=null;this.src='${logoMainPng}'`} />
    </a>

    <ul class="main-navbar__links" id="mainNavbarLinks">
      {localizedNavItems.map((item) => (
        <li>
          <a
            href={item.href}
            class:list={['main-navbar__link', item.active && 'is-active']}
            aria-current={item.active ? 'page' : undefined}
          >
            <Icon name={item.icon} size={18} strokeWidth={2.1} />
            {item.label}
          </a>
        </li>
      ))}
    </ul>

    <div class="main-navbar__actions">
      <div class="main-navbar-lang" id="mainNavbarLang">
        <button
          class="main-navbar-lang__toggle"
          id="mainNavbarLangToggle"
          type="button"
          aria-label={t.nav.changeLanguage}
          aria-haspopup="menu"
          aria-expanded="false"
          aria-controls="mainNavbarLangMenu"
        >
          <span class:list={['flag-icon', 'main-navbar-flag', 'fi', currentLanguage.flagClass]} data-code={currentLanguage.code.toUpperCase()} aria-hidden="true"></span>
          <span>{currentLanguage.code.toUpperCase()}</span>
          <Icon name="chevronDown" size={15} strokeWidth={2.4} />
        </button>

        <div class="main-navbar-lang__menu" id="mainNavbarLangMenu" role="menu" aria-label={t.nav.languageList}>
          {languageLinks.map((language) => (
            <a
              href={language.href}
              class:list={['main-navbar-lang__item', language.active && 'is-active']}
              role="menuitem"
              aria-current={language.active ? 'page' : undefined}
            >
              <span class:list={['flag-icon', 'main-navbar-flag', 'fi', language.flagClass]} data-code={language.code.toUpperCase()} aria-hidden="true"></span>
              <span>{language.name}</span>
              <strong>{language.code.toUpperCase()}</strong>
            </a>
          ))}
        </div>
      </div>

      <button
        class="main-navbar-search"
        type="button"
        data-search-open
        aria-label={pathContext.languageCode === 'pl' ? 'Szukaj na stronie' : 'Search the site'}
      >
        <Icon name="search" size={18} strokeWidth={2.1} />
      </button>

      <button
        class="main-navbar-theme"
        type="button"
        data-theme-toggle
        aria-label={t.nav.changeTheme}
        aria-pressed="false"
      >
        <span class="main-navbar-theme__icon main-navbar-theme__icon--sun" aria-hidden="true">
          <Icon name="sun" size={19} strokeWidth={2} />
        </span>
        <span class="main-navbar-theme__icon main-navbar-theme__icon--moon" aria-hidden="true">
          <Icon name="moon" size={19} strokeWidth={2} />
        </span>
      </button>

      <a href={bookingHref} class="main-navbar__cta">{t.nav.cta}</a>
    </div>

    <div class="main-navbar__mobile-controls" aria-label={t.nav.mobileMenu}>
      <button
        class="main-navbar-search main-navbar-search--compact"
        type="button"
        data-search-open
        aria-label={pathContext.languageCode === 'pl' ? 'Szukaj na stronie' : 'Search the site'}
      >
        <Icon name="search" size={18} strokeWidth={2.1} />
      </button>

      <button
        class="main-navbar-theme main-navbar-theme--compact"
        type="button"
        data-theme-toggle
        aria-label={t.nav.changeTheme}
        aria-pressed="false"
      >
        <span class="main-navbar-theme__icon main-navbar-theme__icon--sun" aria-hidden="true">
          <Icon name="sun" size={18} strokeWidth={2} />
        </span>
        <span class="main-navbar-theme__icon main-navbar-theme__icon--moon" aria-hidden="true">
          <Icon name="moon" size={18} strokeWidth={2} />
        </span>
      </button>

      <button
        class="main-navbar__hamburger"
        id="mainNavbarToggle"
        type="button"
        data-mobile-toggle
        aria-label={t.nav.openMenu}
        aria-expanded="false"
        aria-controls="mainNavbarMobile"
      >
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </nav>

  <div class="main-navbar-mobile" id="mainNavbarMobile" aria-hidden="true">
    <div class="main-navbar-mobile__panel" role="dialog" aria-modal="true" aria-label={t.nav.mobileMenu}>
      <div class="main-navbar-mobile__top">
        <a href={homeHref} class="main-navbar-mobile__brand" aria-label={t.nav.homeAria}>
          <img src={logoMain} alt="Camping Clepardia" width="180" height="44" onerror={`this.onerror=null;this.src='${logoMainPng}'`} />
        </a>
        <button class="main-navbar-mobile__close" type="button" data-mobile-close aria-label={t.nav.closeMenu}>
          <Icon name="close" size={22} strokeWidth={2.2} />
        </button>
      </div>

      <ul class="main-navbar-mobile__links">
        {localizedNavItems.map((item) => (
          <li>
            <a
              href={item.href}
              class:list={['main-navbar-mobile__link', item.active && 'is-active']}
            aria-current={item.active ? 'page' : undefined}
          >
              <Icon name={item.icon} size={18} strokeWidth={2.05} />
              {item.label}
            </a>
          </li>
        ))}
      </ul>

      <div class="main-navbar-mobile__bottom">
        <button
          class="main-navbar-theme main-navbar-theme--mobile"
          type="button"
          data-theme-toggle
          aria-label={t.nav.changeTheme}
          aria-pressed="false"
        >
          <span class="main-navbar-theme__icon main-navbar-theme__icon--sun" aria-hidden="true">
            <Icon name="sun" size={19} strokeWidth={2} />
          </span>
          <span class="main-navbar-theme__icon main-navbar-theme__icon--moon" aria-hidden="true">
            <Icon name="moon" size={19} strokeWidth={2} />
          </span>
          <span data-theme-label>{t.nav.lightMode}</span>
        </button>

        <div class="main-navbar-mobile__languages" aria-label={t.nav.chooseLanguage}>
          {languageLinks.map((language) => (
            <a
              href={language.href}
              class:list={['main-navbar-mobile__language', language.active && 'is-active']}
              aria-label={`${language.name} ${language.code.toUpperCase()}`}
              aria-current={language.active ? 'page' : undefined}
            >
              <span class:list={['flag-icon', 'main-navbar-flag', 'fi', language.flagClass]} data-code={language.code.toUpperCase()} aria-hidden="true"></span>
              <span>{language.code.toUpperCase()}</span>
            </a>
          ))}
        </div>
        <a href={bookingHref} class="main-navbar__cta main-navbar__cta--mobile">{t.nav.cta}</a>
      </div>
    </div>
  </div>
</header>

<script define:vars={{ navMessages }}>
  (() => {
    const shell = document.getElementById('mainNavbarShell');
    const toggle = document.getElementById('mainNavbarToggle');
    const mobile = document.getElementById('mainNavbarMobile');
    const panel = mobile?.querySelector('.main-navbar-mobile__panel');
    const lang = document.getElementById('mainNavbarLang');
    const langToggle = document.getElementById('mainNavbarLangToggle');
    const langMenu = document.getElementById('mainNavbarLangMenu');
    const mobileLinks = document.querySelectorAll('.main-navbar-mobile a, .main-navbar__cta--mobile');
    const mobileCloseButtons = document.querySelectorAll('[data-mobile-close]');
    const themeToggles = document.querySelectorAll('[data-theme-toggle]');

    let mobileOpen = false;
    let langOpen = false;
    const themeWindow = window;

    const setLangOpen = (open) => {
      langOpen = open;
      lang?.classList.toggle('is-open', open);
      langToggle?.setAttribute('aria-expanded', String(open));
    };

    const setMobileOpen = (open) => {
      mobileOpen = open;
      mobile?.classList.toggle('is-open', open);
      toggle?.classList.toggle('is-open', open);
      shell?.classList.toggle('has-mobile-open', open);
      mobile?.setAttribute('aria-hidden', String(!open));
      toggle?.setAttribute('aria-expanded', String(open));
      toggle?.setAttribute('aria-label', open ? navMessages.closeMenu : navMessages.openMenu);
      document.body.classList.toggle('has-main-navbar-open', open);

      if (open) {
        setLangOpen(false);
        window.setTimeout(() => {
          const firstLink = mobile?.querySelector('a');
          if (firstLink instanceof HTMLElement) firstLink.focus({ preventScroll: true });
        }, 120);
      }
    };

    const updateScrolled = () => {
      shell?.classList.toggle('is-scrolled', window.scrollY > 24);
    };

    const syncThemeToggles = () => {
      const activeTheme = document.documentElement.dataset.theme === 'light' ? 'light' : 'dark';
      const nextTheme = activeTheme === 'dark' ? 'light' : 'dark';
      themeToggles.forEach((button) => {
        if (!(button instanceof HTMLButtonElement)) return;
        button.setAttribute('aria-pressed', String(activeTheme === 'light'));
        button.setAttribute('aria-label', nextTheme === 'light' ? navMessages.enableLight : navMessages.enableDark);
        button.dataset.currentTheme = activeTheme;
        const label = button.querySelector('[data-theme-label]');
        if (label) label.textContent = nextTheme === 'light' ? navMessages.lightMode : navMessages.darkMode;
      });
    };

    themeToggles.forEach((button) => {
      button.addEventListener('click', () => {
        const activeTheme = document.documentElement.dataset.theme === 'light' ? 'light' : 'dark';
        const nextTheme = activeTheme === 'dark' ? 'light' : 'dark';

        if (typeof themeWindow.__ccSetTheme === 'function') {
          themeWindow.__ccSetTheme(nextTheme, true);
        } else {
          document.documentElement.dataset.theme = nextTheme;
          try {
            window.localStorage.setItem('cc-theme', nextTheme);
          } catch {}
        }

        syncThemeToggles();
      });
    });

    toggle?.addEventListener('click', () => setMobileOpen(!mobileOpen));

    mobileCloseButtons.forEach((button) => {
      button.addEventListener('click', () => setMobileOpen(false));
    });

    mobile?.addEventListener('click', (event) => {
      if (event.target === mobile) setMobileOpen(false);
    });

    panel?.addEventListener('click', (event) => {
      event.stopPropagation();
    });

    mobileLinks.forEach((link) => {
      link.addEventListener('click', () => setMobileOpen(false));
    });

    langToggle?.addEventListener('click', (event) => {
      event.stopPropagation();
      setLangOpen(!langOpen);
    });

    langMenu?.addEventListener('click', (event) => {
      event.stopPropagation();
      setLangOpen(false);
    });

    document.addEventListener('click', (event) => {
      const target = event.target;
      if (!(target instanceof Node) || !lang?.contains(target)) setLangOpen(false);
    });

    window.addEventListener('keydown', (event) => {
      if (event.key !== 'Escape') return;
      setLangOpen(false);
      if (mobileOpen) setMobileOpen(false);
    });

    window.addEventListener('scroll', updateScrolled, { passive: true });
    window.addEventListener('cc-theme-change', syncThemeToggles);
    window.addEventListener('resize', () => {
      if (window.innerWidth > 920 && mobileOpen) setMobileOpen(false);
    });

    updateScrolled();
    syncThemeToggles();
  })();
</script>

<style is:global>
body.has-main-navbar-open {
  overflow: hidden;
}

.main-navbar-shell {
  position: fixed;
  inset: 0 0 auto;
  z-index: 1000;
  width: 100%;
  pointer-events: none;
}

.main-navbar-shell.has-mobile-open {
  z-index: 1200;
}

.main-navbar {
  position: relative;
  z-index: 2;
  width: min(1480px, calc(100% - 28px));
  height: 86px;
  margin: 16px auto 0;
  padding: 0 22px;
  display: grid;
  grid-template-columns: minmax(228px, auto) minmax(0, 1fr) auto;
  align-items: center;
  gap: 26px;
  border: 1px solid rgba(143,224,180,.24);
  border-radius: 999px;
  background:
    radial-gradient(circle at 12% 0%, rgba(60,179,113,.24), transparent 36%),
    radial-gradient(circle at 88% 6%, rgba(242,192,120,.12), transparent 34%),
    linear-gradient(135deg, rgba(8, 29, 20, .88), rgba(15, 43, 29, .78));
  box-shadow:
    0 26px 74px rgba(0,0,0,.24),
    0 0 42px rgba(60,179,113,.16),
    inset 0 1px 0 rgba(255,255,255,.13);
  backdrop-filter: blur(22px) saturate(140%);
  -webkit-backdrop-filter: blur(22px) saturate(140%);
  pointer-events: auto;
  transition: height .28s ease, margin .28s ease, background .28s ease, box-shadow .28s ease, border-color .28s ease;
}

.main-navbar-shell.is-scrolled .main-navbar {
  height: 70px;
  margin-top: 10px;
  background:
    radial-gradient(circle at 10% 0%, rgba(60,179,113,.18), transparent 32%),
    linear-gradient(135deg, rgba(9, 34, 23, .94), rgba(14, 43, 29, .90));
  border-color: rgba(143,224,180,.30);
  box-shadow: 0 20px 60px rgba(0,0,0,.26), 0 0 34px rgba(60,179,113,.14), inset 0 1px 0 rgba(255,255,255,.12);
}

.main-navbar__brand,
.main-navbar-mobile__brand {
  display: inline-flex;
  align-items: center;
  min-width: 0;
}

.main-navbar__logo {
  display: block;
  height: 60px;
  max-width: 246px;
  width: auto;
  object-fit: contain;
  image-rendering: auto;
  filter: drop-shadow(0 12px 28px rgba(0,0,0,.24)) drop-shadow(0 0 18px rgba(60,179,113,.12));
  transition: height .28s ease;
}

.main-navbar-shell.is-scrolled .main-navbar__logo {
  height: 54px;
}

.main-navbar__links {
  justify-self: center;
  display: flex;
  align-items: center;
  gap: 7px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.main-navbar__link {
  display: inline-flex;
  min-height: 46px;
  align-items: center;
  justify-content: center;
  gap: 7px;
  padding: 0 13px;
  border: 1px solid transparent;
  border-radius: 999px;
  color: rgba(255,255,255,.82);
  font-size: 15px;
  font-weight: 820;
  letter-spacing: -.01em;
  white-space: nowrap;
  transition: color .22s ease, background .22s ease, border-color .22s ease, box-shadow .22s ease, transform .22s ease;
}

.main-navbar__link svg {
  width: 18px;
  height: 18px;
  opacity: .76;
  transition: opacity .22s ease, transform .22s ease, filter .22s ease;
}

.main-navbar__link:hover svg,
.main-navbar__link.is-active svg {
  opacity: 1;
  transform: translateY(-1px);
  filter: drop-shadow(0 0 10px rgba(143,224,180,.28));
}

.main-navbar__link:hover {
  color: #fff;
  border-color: rgba(143,224,180,.18);
  background: rgba(60,179,113,.15);
  box-shadow: 0 0 34px rgba(60,179,113,.22);
  transform: translateY(-1px);
}

.main-navbar__link.is-active {
  color: #fff;
  background:
    linear-gradient(135deg, rgba(60,179,113,.32), rgba(60,179,113,.16));
  border-color: rgba(143,224,180,.52);
  box-shadow: 0 0 42px rgba(60,179,113,.30), inset 0 1px 0 rgba(255,255,255,.12);
}

.main-navbar__actions {
  justify-self: end;
  display: flex;
  align-items: center;
  gap: 12px;
}

.main-navbar-lang {
  position: relative;
}

.main-navbar-lang__toggle {
  min-height: 48px;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 14px;
  border: 1px solid rgba(143,224,180,.24);
  border-radius: 999px;
  background:
    linear-gradient(145deg, rgba(60,179,113,.11), rgba(255,255,255,.055)),
    rgba(255,255,255,.06);
  color: #fff;
  box-shadow: 0 10px 28px rgba(60,179,113,.08), inset 0 1px 0 rgba(255,255,255,.09);
  font-size: 14px;
  font-weight: 800;
  letter-spacing: .03em;
  transition: background .22s ease, border-color .22s ease, transform .22s ease;
}

.main-navbar-lang__toggle:hover,
.main-navbar-lang.is-open .main-navbar-lang__toggle {
  background: rgba(60,179,113,.14);
  border-color: rgba(143,224,180,.42);
  transform: translateY(-1px);
}

.main-navbar-flag {
  display: inline-block;
  flex: 0 0 auto;
  border-radius: 5px;
  border: 1px solid rgba(143,224,180,.30);
  background-color: transparent;
  box-shadow: 0 5px 14px rgba(0,0,0,.22), 0 0 0 3px rgba(60,179,113,.06);
  overflow: hidden;
  background-size: cover !important;
  background-position: center;
  background-repeat: no-repeat !important;
}

.main-navbar-flag::before {
  border-radius: inherit;
  background-size: cover;
  background-position: center;
}

.main-navbar-lang__toggle .main-navbar-flag {
  width: 18px;
  height: 14px;
}

.main-navbar-lang__item .main-navbar-flag {
  width: 18px;
  height: 14px;
}

.main-navbar-lang__toggle svg {
  width: 15px;
  height: 15px;
  fill: none;
  stroke: currentColor;
  stroke-width: 2.4;
  stroke-linecap: round;
  stroke-linejoin: round;
  opacity: .78;
  transition: transform .22s ease;
}

.main-navbar-lang.is-open .main-navbar-lang__toggle svg {
  transform: rotate(180deg);
}

.main-navbar-lang__menu {
  position: absolute;
  top: calc(100% + 14px);
  right: 0;
  width: 292px;
  padding: 10px;
  border: 1px solid rgba(255,255,255,.13);
  border-radius: 24px;
  background:
    radial-gradient(circle at 20% 0%, rgba(60,179,113,.16), transparent 36%),
    radial-gradient(circle at 88% 100%, rgba(242,192,120,.10), transparent 40%),
    rgba(9, 30, 21, .96);
  box-shadow: 0 30px 82px rgba(0,0,0,.30), 0 0 28px rgba(60,179,113,.10), inset 0 1px 0 rgba(255,255,255,.12);
  backdrop-filter: blur(24px) saturate(140%);
  -webkit-backdrop-filter: blur(24px) saturate(140%);
  opacity: 0;
  visibility: hidden;
  transform: translateY(-8px) scale(.98);
  transform-origin: top right;
  transition: opacity .22s ease, visibility .22s ease, transform .22s ease;
}

.main-navbar-lang.is-open .main-navbar-lang__menu {
  opacity: 1;
  visibility: visible;
  transform: translateY(0) scale(1);
}

.main-navbar-lang__item {
  display: grid;
  grid-template-columns: 18px 1fr auto;
  align-items: center;
  gap: 10px;
  padding: 11px 12px;
  border: 1px solid transparent;
  border-radius: 16px;
  color: rgba(255,255,255,.84);
  font-size: 14px;
  font-weight: 750;
  transition: color .2s ease, background .2s ease, transform .2s ease, border-color .2s ease;
}

.main-navbar-lang__item strong {
  color: #8fe0b4;
  font-size: 12px;
  letter-spacing: .06em;
}

.main-navbar-lang__item:hover,
.main-navbar-lang__item.is-active {
  color: #fff;
  border-color: rgba(60,179,113,.24);
  background: rgba(60,179,113,.16);
  transform: translateX(2px);
}

.main-navbar-theme {
  width: 48px;
  height: 48px;
  display: inline-grid;
  place-items: center;
  border: 1px solid rgba(255,255,255,.12);
  border-radius: 999px;
  color: #fff;
  background: rgba(255,255,255,.075);
  box-shadow: inset 0 1px 0 rgba(255,255,255,.08);
  transition: transform .22s ease, border-color .22s ease, background .22s ease, box-shadow .22s ease, color .22s ease;
}

.main-navbar-search {
  width: 48px;
  height: 48px;
  display: inline-grid;
  place-items: center;
  border: 1px solid rgba(255,255,255,.12);
  border-radius: 999px;
  color: #fff;
  background:
    radial-gradient(circle at 30% 15%, rgba(143,224,180,.18), transparent 36%),
    rgba(255,255,255,.075);
  box-shadow: inset 0 1px 0 rgba(255,255,255,.08);
  cursor: pointer;
  transition: transform .22s ease, border-color .22s ease, background .22s ease, box-shadow .22s ease, color .22s ease;
}

.main-navbar-search:hover {
  color: #fff;
  transform: translateY(-1px);
  border-color: rgba(143,224,180,.42);
  background: rgba(255,255,255,.13);
  box-shadow: 0 0 28px rgba(60,179,113,.16), inset 0 1px 0 rgba(255,255,255,.12);
}

.main-navbar-theme:hover {
  color: #fff;
  transform: translateY(-1px);
  border-color: rgba(143,224,180,.42);
  background: rgba(255,255,255,.13);
  box-shadow: 0 0 28px rgba(60,179,113,.16), inset 0 1px 0 rgba(255,255,255,.12);
}

.main-navbar-theme__icon {
  grid-area: 1 / 1;
  display: grid;
  place-items: center;
  transition: opacity .24s ease, transform .24s ease;
}

.main-navbar-theme svg {
  width: 19px;
  height: 19px;
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

html[data-theme="dark"] .main-navbar-theme__icon--moon,
html[data-theme="light"] .main-navbar-theme__icon--sun {
  opacity: 0;
  transform: scale(.72) rotate(-24deg);
}

html[data-theme="dark"] .main-navbar-theme__icon--sun,
html[data-theme="light"] .main-navbar-theme__icon--moon {
  opacity: 1;
  transform: scale(1) rotate(0deg);
}

.main-navbar-theme--mobile {
  width: 100%;
  height: 56px;
  grid-template-columns: auto auto 1fr;
  justify-content: start;
  gap: 10px;
  padding: 0 16px;
  border-radius: 18px;
  color: rgba(255,255,255,.92);
  font-weight: 850;
  letter-spacing: .02em;
}

.main-navbar-theme--mobile .main-navbar-theme__icon {
  position: relative;
  grid-area: auto;
}

.main-navbar-theme--mobile .main-navbar-theme__icon--moon {
  margin-left: -29px;
}

.main-navbar-theme--mobile [data-theme-label] {
  margin-left: 4px;
  text-align: left;
}

.main-navbar__cta {
  position: relative;
  overflow: hidden;
  display: inline-flex;
  min-height: 50px;
  align-items: center;
  justify-content: center;
  padding: 0 26px;
  border-radius: 999px;
  color: #fff;
  background: linear-gradient(135deg, #7ce7b2 0%, #3CB371 45%, #1f744b 100%);
  box-shadow: 0 20px 52px rgba(60,179,113,.44), 0 0 24px rgba(60,179,113,.18), inset 0 1px 0 rgba(255,255,255,.28);
  font-size: 14.5px;
  font-weight: 950;
  letter-spacing: .01em;
  white-space: nowrap;
  transition: transform .22s ease, box-shadow .22s ease;
}

.main-navbar__cta::before {
  content: '';
  position: absolute;
  inset: -80% auto -80% -35%;
  width: 34%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,.48), transparent);
  transform: translateX(-140%) rotate(18deg);
  transition: transform .72s ease;
}

.main-navbar__cta:hover {
  color: #fff;
  transform: translateY(-2px);
  box-shadow: 0 22px 58px rgba(60,179,113,.48), inset 0 1px 0 rgba(255,255,255,.26);
}

.main-navbar__cta:hover::before {
  transform: translateX(430%) rotate(18deg);
}

.main-navbar__hamburger {
  display: none;
  width: 48px;
  height: 48px;
  justify-self: end;
  place-content: center;
  gap: 5px;
  border: 1px solid rgba(255,255,255,.12);
  border-radius: 16px;
  background: rgba(255,255,255,.08);
  transition: background .22s ease, border-color .22s ease;
}

.main-navbar__mobile-controls {
  display: none;
  justify-self: end;
  align-items: center;
  gap: 8px;
}

.main-navbar__mobile-controls .main-navbar__hamburger {
  display: grid;
}

.main-navbar-theme--compact {
  width: 46px;
  height: 46px;
  border-radius: 16px;
}

.main-navbar__hamburger span {
  display: block;
  width: 23px;
  height: 2px;
  border-radius: 99px;
  background: #fff;
  transition: transform .25s ease, opacity .2s ease;
}

.main-navbar__hamburger.is-open {
  background: rgba(255,255,255,.14);
  border-color: rgba(143,224,180,.38);
}

.main-navbar__hamburger.is-open span:first-child {
  transform: translateY(7px) rotate(45deg);
}

.main-navbar__hamburger.is-open span:nth-child(2) {
  opacity: 0;
}

.main-navbar__hamburger.is-open span:last-child {
  transform: translateY(-7px) rotate(-45deg);
}

.main-navbar-mobile {
  position: fixed;
  inset: 0;
  z-index: 1200;
  width: 100%;
  max-width: 100vw;
  pointer-events: auto;
  opacity: 0;
  visibility: hidden;
  overflow: hidden;
  overscroll-behavior: contain;
  touch-action: pan-y;
  background:
    radial-gradient(circle at 14% 12%, rgba(60,179,113,.20), transparent 34%),
    radial-gradient(circle at 82% 78%, rgba(242,192,120,.16), transparent 38%),
    rgba(7, 20, 15, .86);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  transition: opacity .28s ease, visibility .28s ease;
}

.main-navbar-mobile.is-open {
  opacity: 1;
  visibility: visible;
}

.main-navbar-mobile__panel {
  width: 100%;
  max-width: 100vw;
  height: 100dvh;
  max-height: 100dvh;
  min-height: 0;
  display: flex;
  flex-direction: column;
  padding: 104px 24px max(28px, env(safe-area-inset-bottom));
  overflow-x: hidden;
  overflow-y: auto;
  overscroll-behavior: contain;
  -webkit-overflow-scrolling: touch;
  box-sizing: border-box;
  touch-action: pan-y;
  opacity: 0;
  transform: translateY(20px);
  transition: opacity .34s ease .04s, transform .34s ease .04s;
}

.main-navbar-mobile.is-open .main-navbar-mobile__panel {
  opacity: 1;
  transform: translateY(0);
}

.main-navbar-mobile__top {
  position: relative;
  display: grid;
  grid-template-columns: 48px minmax(0, 1fr) 48px;
  align-items: center;
  gap: 12px;
  margin-bottom: 26px;
}

.main-navbar-mobile__brand {
  grid-column: 2;
  justify-self: center;
}

.main-navbar-mobile__brand img {
  height: 42px;
  width: auto;
  object-fit: contain;
  image-rendering: auto;
  filter: drop-shadow(0 18px 38px rgba(60,179,113,.24));
}

.main-navbar-mobile__close {
  position: fixed;
  top: max(22px, env(safe-area-inset-top));
  right: max(18px, env(safe-area-inset-right));
  z-index: 4;
  width: 48px;
  height: 48px;
  display: grid;
  place-items: center;
  border: 1px solid rgba(143,224,180,.28);
  border-radius: 17px;
  color: #fff;
  background:
    radial-gradient(circle at 28% 18%, rgba(255,255,255,.16), transparent 38%),
    rgba(11, 34, 23, .78);
  box-shadow: 0 18px 44px rgba(0,0,0,.22), 0 0 24px rgba(60,179,113,.14), inset 0 1px 0 rgba(255,255,255,.10);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  transition: transform .22s ease, border-color .22s ease, background .22s ease;
}

.main-navbar-mobile__close:hover {
  transform: translateY(-1px);
  border-color: rgba(143,224,180,.48);
  background: rgba(60,179,113,.18);
}

.main-navbar-mobile__close svg {
  width: 22px;
  height: 22px;
  fill: none;
  stroke: currentColor;
  stroke-width: 2.2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.main-navbar-mobile__links {
  display: grid;
  gap: 0;
  margin: 0;
  padding: 0;
  list-style: none;
}

.main-navbar-mobile__link svg {
  width: 18px;
  height: 18px;
  color: #8fe0b4;
}

.main-navbar-mobile__link {
  display: flex;
  min-height: 58px;
  align-items: center;
  border-bottom: 1px solid rgba(255,255,255,.10);
  color: #fff;
  font-size: 28px;
  font-weight: 800;
  line-height: 1;
  letter-spacing: -.02em;
}

.main-navbar-mobile__link.is-active {
  color: #8fe0b4;
}

.main-navbar-mobile__bottom {
  margin-top: auto;
  display: grid;
  gap: 20px;
  padding-top: 28px;
}

.main-navbar-mobile__languages {
  display: grid;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  gap: 9px;
}

.main-navbar-mobile__language {
  min-height: 54px;
  display: grid;
  place-items: center;
  gap: 4px;
  padding: 8px 4px;
  border: 1px solid rgba(255,255,255,.12);
  border-radius: 18px;
  background:
    linear-gradient(145deg, rgba(255,255,255,.08), rgba(255,255,255,.045)),
    rgba(255,255,255,.06);
  color: rgba(255,255,255,.84);
  font-size: 11px;
  font-weight: 850;
  letter-spacing: .08em;
  transition: background .22s ease, border-color .22s ease, transform .22s ease;
}

.main-navbar-mobile__language:hover,
.main-navbar-mobile__language.is-active {
  color: #fff;
  border-color: rgba(143,224,180,.44);
  background: rgba(60,179,113,.18);
  transform: translateY(-2px);
}

.main-navbar__cta--mobile {
  width: 100%;
  min-height: 56px;
  font-size: 14px;
}

.main-navbar a:focus-visible,
.main-navbar button:focus-visible,
.main-navbar-mobile a:focus-visible,
.main-navbar-mobile button:focus-visible {
  outline: 2px solid #8fe0b4;
  outline-offset: 4px;
}

html[data-theme="light"] .main-navbar {
  border-color: rgba(25, 54, 38, .13);
  background:
    radial-gradient(circle at 10% 0%, rgba(60,179,113,.13), transparent 34%),
    radial-gradient(circle at 88% 6%, rgba(242,192,120,.10), transparent 34%),
    rgba(247, 246, 238, .82);
  box-shadow: 0 20px 58px rgba(42, 62, 45, .17), 0 0 28px rgba(60,179,113,.08), inset 0 1px 0 rgba(255,255,255,.74);
}

html[data-theme="light"] .main-navbar-shell.is-scrolled .main-navbar {
  border-color: rgba(25, 54, 38, .17);
  background: rgba(247, 246, 238, .90);
}

html[data-theme="light"] .main-navbar__link,
html[data-theme="light"] .main-navbar-lang__toggle,
html[data-theme="light"] .main-navbar-search,
html[data-theme="light"] .main-navbar-theme {
  color: #173624;
  border-color: rgba(25, 54, 38, .12);
  background: rgba(255,255,255,.48);
}

html[data-theme="light"] .main-navbar__link:hover,
html[data-theme="light"] .main-navbar-lang__toggle:hover,
html[data-theme="light"] .main-navbar-lang.is-open .main-navbar-lang__toggle,
html[data-theme="light"] .main-navbar-search:hover,
html[data-theme="light"] .main-navbar-theme:hover {
  color: #102018;
  border-color: rgba(60,179,113,.28);
  background: rgba(60,179,113,.12);
}

html[data-theme="light"] .main-navbar__link.is-active {
  color: #102018;
  background: rgba(60,179,113,.17);
}

html[data-theme="light"] .main-navbar-lang__menu {
  border-color: rgba(25, 54, 38, .12);
  background: rgba(247,246,238,.96);
  box-shadow: 0 28px 70px rgba(42, 62, 45, .20), inset 0 1px 0 rgba(255,255,255,.78);
}

html[data-theme="light"] .main-navbar-lang__item {
  color: #244332;
}

html[data-theme="light"] .main-navbar-lang__item:hover,
html[data-theme="light"] .main-navbar-lang__item.is-active {
  color: #102018;
}

html[data-theme="light"] .main-navbar__hamburger {
  border-color: rgba(25, 54, 38, .14);
  background: rgba(255,255,255,.52);
}

html[data-theme="light"] .main-navbar__hamburger span {
  background: #173624;
}

html[data-theme="light"] .main-navbar-mobile {
  background:
    radial-gradient(circle at 14% 12%, rgba(60,179,113,.22), transparent 34%),
    radial-gradient(circle at 82% 78%, rgba(239,174,99,.12), transparent 38%),
    rgba(247,246,238,.86);
}

html[data-theme="light"] .main-navbar-mobile__link,
html[data-theme="light"] .main-navbar-theme--mobile {
  color: #102018;
}

html[data-theme="light"] .main-navbar-mobile__link {
  border-bottom-color: rgba(25, 54, 38, .12);
}

html[data-theme="light"] .main-navbar-mobile__language {
  color: #173624;
  border-color: rgba(25, 54, 38, .12);
  background: rgba(255,255,255,.56);
}

html[data-theme="light"] .main-navbar-mobile__close {
  color: #102018;
  border-color: rgba(25,54,38,.14);
  background: rgba(247,246,238,.78);
  box-shadow: 0 18px 42px rgba(42,62,45,.16), 0 0 24px rgba(60,179,113,.10), inset 0 1px 0 rgba(255,255,255,.72);
}

@media (max-width: 1380px) and (min-width: 921px) {
  .main-navbar {
    gap: 12px;
    padding-inline: 16px;
  }

  .main-navbar__link {
    min-height: 42px;
    padding-inline: 9px;
    font-size: 13px;
    gap: 5px;
  }

  .main-navbar__link svg {
    width: 16px;
    height: 16px;
  }

  .main-navbar__cta {
    padding-inline: 17px;
    font-size: 13px;
  }
}

@media (max-width: 920px) {
  .main-navbar {
    width: calc(100% - 24px);
    max-width: calc(100vw - 24px);
    height: 68px;
    margin-top: 12px;
    grid-template-columns: auto 1fr auto;
    gap: 8px;
    border-radius: 24px;
    padding: 0 14px;
  }

  .main-navbar-shell.is-scrolled .main-navbar {
    height: 62px;
    margin-top: 8px;
  }

  .main-navbar__logo {
    height: 46px;
    max-width: min(178px, calc(100vw - 206px));
  }

  .main-navbar-shell.is-scrolled .main-navbar__logo {
    height: 42px;
  }

  .main-navbar__links,
  .main-navbar__actions {
    display: none;
  }

  .main-navbar > .main-navbar__hamburger {
    display: none;
  }

  .main-navbar__mobile-controls {
    display: flex;
  }
}

@media (max-width: 430px) {
  .main-navbar-mobile__panel {
    padding: 92px 18px max(24px, env(safe-area-inset-bottom));
  }

  .main-navbar-mobile__languages {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }
}

@media (max-width: 380px) {
  .main-navbar {
    width: calc(100% - 20px);
    max-width: calc(100vw - 20px);
    padding-inline: 12px;
    gap: 6px;
  }

  .main-navbar__logo {
    height: 42px;
    max-width: min(150px, calc(100vw - 220px));
  }

  .main-navbar-mobile__link {
    min-height: 54px;
    font-size: clamp(1.42rem, 7.5vw, 1.75rem);
  }
}

/* Task 48: final clean premium navbar */
.main-navbar {
  border-color: rgba(143,224,180,.20);
  background:
    linear-gradient(145deg, rgba(7, 24, 16, .82), rgba(13, 37, 25, .72)),
    radial-gradient(circle at 12% 0%, rgba(60,179,113,.18), transparent 30%),
    radial-gradient(circle at 88% 0%, rgba(242,192,120,.10), transparent 28%);
  box-shadow:
    0 24px 72px rgba(0,0,0,.24),
    0 0 34px rgba(60,179,113,.15),
    inset 0 1px 0 rgba(255,255,255,.14);
}

.main-navbar__logo {
  filter: drop-shadow(0 10px 20px rgba(60,179,113,.14));
}

.main-navbar__link {
  transition:
    color .24s cubic-bezier(.22, 1, .36, 1),
    background .24s cubic-bezier(.22, 1, .36, 1),
    border-color .24s cubic-bezier(.22, 1, .36, 1),
    box-shadow .24s cubic-bezier(.22, 1, .36, 1),
    transform .24s cubic-bezier(.22, 1, .36, 1);
}

.main-navbar__link:hover {
  transform: translateY(-1px);
  box-shadow: 0 10px 28px rgba(60,179,113,.12);
}

.main-navbar__link.is-active {
  box-shadow:
    0 10px 30px rgba(60,179,113,.18),
    inset 0 1px 0 rgba(255,255,255,.12);
}

.main-navbar-flag:not([class*="fi-"])::after {
  content: attr(data-code);
  display: grid;
  place-items: center;
  width: 100%;
  height: 100%;
  color: #fff;
  font-size: 10px;
  font-weight: 950;
  letter-spacing: .04em;
}

.main-navbar-flag,
.main-navbar-lang__toggle .main-navbar-flag,
.main-navbar-lang__item .main-navbar-flag,
.main-navbar-mobile__language .main-navbar-flag {
  width: 18px;
  height: 14px;
  flex-shrink: 0;
  border-radius: 2px;
  box-shadow: 0 0 0 1px rgba(255,255,255,.24), 0 8px 18px rgba(0,0,0,.14);
  background-color: transparent;
  background-size: cover !important;
  background-position: 50% 50% !important;
  background-repeat: no-repeat !important;
}

.main-navbar .flag-icon,
.main-navbar-mobile .flag-icon {
  width: 18px;
  height: 14px;
  border-radius: 2px;
  overflow: hidden;
  flex-shrink: 0;
}

.main-navbar-flag::before {
  content: '';
  display: block;
  width: 100%;
  height: 100%;
}

.main-navbar-lang__menu {
  border-color: rgba(143,224,180,.18);
  box-shadow:
    0 26px 72px rgba(0,0,0,.28),
    0 0 34px rgba(60,179,113,.14),
    inset 0 1px 0 rgba(255,255,255,.12);
}

@media (prefers-reduced-motion: reduce) {
  .main-navbar,
  .main-navbar *,
  .main-navbar-mobile,
  .main-navbar-mobile * {
    animation: none !important;
    transition-duration: .01ms !important;
  }
}
</style>
