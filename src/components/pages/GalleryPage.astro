---
import { publicAssetExists, siteAssets } from '@/data/assets';
import Icon from '@/components/ui/Icon.astro';
import { getLocalizedPath } from '@/utils/localizedLinks';
import { getTranslations, resolveContentLanguage } from '@/i18n/config';

interface Props {
  languageCode?: string;
}

type GalleryCategory = 'camping' | 'bungalows' | 'sanitary' | 'transport' | 'krakow';

interface GalleryItem {
  title: string;
  category: GalleryCategory;
  image: string;
  alt: string;
  size: 'standard' | 'wide' | 'tall' | 'feature';
}

const { languageCode } = Astro.props as Props;
const contentLanguage = resolveContentLanguage(languageCode);
const t = getTranslations(languageCode);
const page = t.pages.galeria;
const pricingHref = getLocalizedPath(languageCode, 'cennik');
const contactHref = getLocalizedPath(languageCode, 'kontakt');

const galleryTextByLanguage = {
  pl: {
    headKicker: 'Zdjęcia i miejsca',
    headTitle: 'Camping, domki i okolica w jednym miejscu',
    headCopy:
      'Przeglądaj kategorie i otwieraj zdjęcia w lightboxie. Brakujące materiały są pokazane jako eleganckie miejsca pod zdjęcia.',
    filtersAria: 'Filtry galerii',
    openPhoto: 'Otwórz zdjęcie',
    dialog: 'Podgląd zdjęcia',
    close: 'Zamknij galerię',
    previous: 'Poprzednie zdjęcie',
    next: 'Następne zdjęcie',
    photoComingSoon: 'Zdjęcie w przygotowaniu',
    ctaKicker: 'Zaplanuj pobyt',
    category: {
      all: 'Wszystkie',
      camping: 'Camping',
      bungalows: 'Domki',
      sanitary: 'Sanitariaty',
      transport: 'Dojazd',
      krakow: 'Kraków',
    },
    items: [
      ['Miejsca campingowe', 'Miejsca campingowe Camping Clepardia'],
      ['Strefa dla kamperów', 'Strefa dla kamperów na Camping Clepardia'],
      ['Strefa namiotowa', 'Strefa namiotowa Camping Clepardia'],
      ['Prąd i zaplecze', 'Przyłącza prądu na campingu'],
      ['Domek z zewnątrz', 'Domek Camping Clepardia z zewnątrz'],
      ['Wnętrze domku', 'Wnętrze domku Camping Clepardia'],
      ['Domek 3-osobowy', 'Domek 3-osobowy Camping Clepardia'],
      ['Domek 4-osobowy', 'Domek 4-osobowy Camping Clepardia'],
      ['Budynek sanitarny', 'Budynek sanitarny na campingu'],
      ['Prysznice', 'Prysznice dla gości Camping Clepardia'],
      ['Kuchnia turystyczna', 'Kuchnia turystyczna dla gości'],
      ['Pralnia', 'Pralnia na campingu'],
      ['Tramwaj do centrum', 'Dojazd tramwajem do centrum Krakowa'],
      ['Bilety tramwajowe', 'Automat biletowy w tramwaju'],
      ['Stare Miasto', 'Stare Miasto w Krakowie'],
      ['Kopalnia Soli Wieliczka', 'Kopalnia Soli Wieliczka'],
      ['Energylandia', 'Energylandia'],
    ],
  },
  en: {
    headKicker: 'Photos and places',
    headTitle: 'Campsite, bungalows and surroundings in one place',
    headCopy:
      'Browse categories and open photos in the lightbox. Missing materials are shown as elegant photo placeholders.',
    filtersAria: 'Gallery filters',
    openPhoto: 'Open photo',
    dialog: 'Photo preview',
    close: 'Close gallery',
    previous: 'Previous photo',
    next: 'Next photo',
    photoComingSoon: 'Photo coming soon',
    ctaKicker: 'Plan your stay',
    category: {
      all: 'All',
      camping: 'Camping',
      bungalows: 'Bungalows',
      sanitary: 'Sanitary facilities',
      transport: 'Directions',
      krakow: 'Krakow',
    },
    items: [
      ['Camping pitches', 'Camping Clepardia pitches'],
      ['Motorhome area', 'Motorhome area at Camping Clepardia'],
      ['Tent area', 'Tent area at Camping Clepardia'],
      ['Electricity and facilities', 'Electric hook-ups at the campsite'],
      ['Bungalow exterior', 'Camping Clepardia bungalow exterior'],
      ['Bungalow interior', 'Camping Clepardia bungalow interior'],
      ['3-person bungalow', 'Camping Clepardia 3-person bungalow'],
      ['4-person bungalow', 'Camping Clepardia 4-person bungalow'],
      ['Sanitary building', 'Sanitary building at the campsite'],
      ['Showers', 'Showers for Camping Clepardia guests'],
      ['Tourist kitchen', 'Tourist kitchen for guests'],
      ['Laundry', 'Laundry at the campsite'],
      ['Tram to the centre', 'Tram access to Krakow centre'],
      ['Tram tickets', 'Ticket machine on a tram'],
      ['Old Town', 'Krakow Old Town'],
      ['Wieliczka Salt Mine', 'Wieliczka Salt Mine'],
      ['Energylandia', 'Energylandia'],
    ],
  },
  de: {
    headKicker: 'Fotos und Orte',
    headTitle: 'Campingplatz, Bungalows und Umgebung an einem Ort',
    headCopy:
      'Durchsuchen Sie Kategorien und öffnen Sie Fotos im Lightbox. Fehlende Materialien erscheinen als elegante Fotoplatzhalter.',
    filtersAria: 'Galeriefilter',
    openPhoto: 'Foto öffnen',
    dialog: 'Fotovorschau',
    close: 'Galerie schließen',
    previous: 'Vorheriges Foto',
    next: 'Nächstes Foto',
    photoComingSoon: 'Foto folgt in Kürze',
    ctaKicker: 'Aufenthalt planen',
    category: {
      all: 'Alle',
      camping: 'Camping',
      bungalows: 'Bungalows',
      sanitary: 'Sanitäranlagen',
      transport: 'Anfahrt',
      krakow: 'Krakau',
    },
    items: [
      ['Campingplätze', 'Stellplätze von Camping Clepardia'],
      ['Wohnmobilbereich', 'Wohnmobilbereich auf Camping Clepardia'],
      ['Zeltbereich', 'Zeltbereich von Camping Clepardia'],
      ['Strom und Infrastruktur', 'Stromanschlüsse auf dem Campingplatz'],
      ['Bungalow von außen', 'Camping Clepardia Bungalow von außen'],
      ['Bungalow innen', 'Camping Clepardia Bungalow innen'],
      ['3-Personen-Bungalow', 'Camping Clepardia 3-Personen-Bungalow'],
      ['4-Personen-Bungalow', 'Camping Clepardia 4-Personen-Bungalow'],
      ['Sanitärgebäude', 'Sanitärgebäude auf dem Campingplatz'],
      ['Duschen', 'Duschen für Gäste von Camping Clepardia'],
      ['Gästeküche', 'Gästeküche für Gäste'],
      ['Waschmöglichkeit', 'Waschmöglichkeit auf dem Campingplatz'],
      ['Straßenbahn ins Zentrum', 'Straßenbahnverbindung ins Zentrum von Krakau'],
      ['Straßenbahntickets', 'Ticketautomat in der Straßenbahn'],
      ['Altstadt', 'Krakauer Altstadt'],
      ['Salzbergwerk Wieliczka', 'Salzbergwerk Wieliczka'],
      ['Energylandia', 'Energylandia'],
    ],
  },
  it: {
    headKicker: 'Foto e luoghi',
    headTitle: 'Camping, bungalow e dintorni in un solo posto',
    headCopy:
      'Sfoglia le categorie e apri le foto nel lightbox. I materiali mancanti sono mostrati come eleganti spazi foto.',
    filtersAria: 'Filtri galleria',
    openPhoto: 'Apri foto',
    dialog: 'Anteprima foto',
    close: 'Chiudi galleria',
    previous: 'Foto precedente',
    next: 'Foto successiva',
    photoComingSoon: 'Foto in arrivo',
    ctaKicker: 'Pianifica il soggiorno',
    category: {
      all: 'Tutte',
      camping: 'Camping',
      bungalows: 'Bungalow',
      sanitary: 'Servizi igienici',
      transport: 'Come arrivare',
      krakow: 'Cracovia',
    },
    items: [
      ['Piazzole camping', 'Piazzole di Camping Clepardia'],
      ['Area camper', 'Area camper a Camping Clepardia'],
      ['Area tende', 'Area tende di Camping Clepardia'],
      ['Elettricità e servizi', 'Allacci elettrici nel camping'],
      ['Bungalow esterno', 'Bungalow Camping Clepardia dall’esterno'],
      ['Interno bungalow', 'Interno bungalow Camping Clepardia'],
      ['Bungalow 3 persone', 'Bungalow 3 persone Camping Clepardia'],
      ['Bungalow 4 persone', 'Bungalow 4 persone Camping Clepardia'],
      ['Edificio servizi', 'Edificio servizi nel camping'],
      ['Docce', 'Docce per gli ospiti di Camping Clepardia'],
      ['Cucina turistica', 'Cucina turistica per ospiti'],
      ['Lavanderia', 'Lavanderia nel camping'],
      ['Tram per il centro', 'Collegamento in tram verso il centro di Cracovia'],
      ['Biglietti tram', 'Distributore biglietti in tram'],
      ['Città Vecchia', 'Città Vecchia di Cracovia'],
      ['Miniera di Sale Wieliczka', 'Miniera di Sale Wieliczka'],
      ['Energylandia', 'Energylandia'],
    ],
  },
};

const galleryText = galleryTextByLanguage[contentLanguage];

const categories: Array<{ id: 'all' | GalleryCategory; label: string }> = [
  { id: 'all', label: galleryText.category.all },
  { id: 'camping', label: galleryText.category.camping },
  { id: 'bungalows', label: galleryText.category.bungalows },
  { id: 'sanitary', label: galleryText.category.sanitary },
  { id: 'transport', label: galleryText.category.transport },
  { id: 'krakow', label: galleryText.category.krakow },
];

const categoryLabels = Object.fromEntries(categories.map((category) => [category.id, category.label])) as Record<string, string>;
const categoryIcons: Record<GalleryCategory, string> = {
  camping: 'tent',
  bungalows: 'bungalow',
  sanitary: 'sanitary',
  transport: 'tram',
  krakow: 'attractions',
};

const galleryItems: GalleryItem[] = [
  {
    title: galleryText.items[0][0],
    category: 'camping',
    image: siteAssets.camping.pitches,
    alt: galleryText.items[0][1],
    size: 'feature',
  },
  {
    title: galleryText.items[1][0],
    category: 'camping',
    image: siteAssets.camping.camperArea,
    alt: galleryText.items[1][1],
    size: 'wide',
  },
  {
    title: galleryText.items[2][0],
    category: 'camping',
    image: siteAssets.camping.tents,
    alt: galleryText.items[2][1],
    size: 'standard',
  },
  {
    title: galleryText.items[3][0],
    category: 'camping',
    image: siteAssets.camping.electricity,
    alt: galleryText.items[3][1],
    size: 'tall',
  },
  {
    title: galleryText.items[4][0],
    category: 'bungalows',
    image: siteAssets.bungalows.exterior,
    alt: galleryText.items[4][1],
    size: 'wide',
  },
  {
    title: galleryText.items[5][0],
    category: 'bungalows',
    image: siteAssets.bungalows.room,
    alt: galleryText.items[5][1],
    size: 'standard',
  },
  {
    title: galleryText.items[6][0],
    category: 'bungalows',
    image: siteAssets.bungalows.bungalow3,
    alt: galleryText.items[6][1],
    size: 'tall',
  },
  {
    title: galleryText.items[7][0],
    category: 'bungalows',
    image: siteAssets.bungalows.bungalow4,
    alt: galleryText.items[7][1],
    size: 'standard',
  },
  {
    title: galleryText.items[8][0],
    category: 'sanitary',
    image: siteAssets.sanitary.building,
    alt: galleryText.items[8][1],
    size: 'wide',
  },
  {
    title: galleryText.items[9][0],
    category: 'sanitary',
    image: siteAssets.sanitary.showers,
    alt: galleryText.items[9][1],
    size: 'standard',
  },
  {
    title: galleryText.items[10][0],
    category: 'sanitary',
    image: siteAssets.sanitary.kitchen,
    alt: galleryText.items[10][1],
    size: 'tall',
  },
  {
    title: galleryText.items[11][0],
    category: 'sanitary',
    image: siteAssets.sanitary.laundry,
    alt: galleryText.items[11][1],
    size: 'standard',
  },
  {
    title: galleryText.items[12][0],
    category: 'transport',
    image: siteAssets.transport.tramToCenter,
    alt: galleryText.items[12][1],
    size: 'wide',
  },
  {
    title: galleryText.items[13][0],
    category: 'transport',
    image: siteAssets.transport.ticketMachine,
    alt: galleryText.items[13][1],
    size: 'standard',
  },
  {
    title: galleryText.items[14][0],
    category: 'krakow',
    image: siteAssets.attractions.oldTown,
    alt: galleryText.items[14][1],
    size: 'feature',
  },
  {
    title: galleryText.items[15][0],
    category: 'krakow',
    image: siteAssets.attractions.wieliczka,
    alt: galleryText.items[15][1],
    size: 'standard',
  },
  {
    title: galleryText.items[16][0],
    category: 'krakow',
    image: siteAssets.attractions.energylandia,
    alt: galleryText.items[16][1],
    size: 'wide',
  },
];

const resolvedGalleryItems = galleryItems.map((item) => ({
  ...item,
  imageExists: publicAssetExists(item.image),
  categoryLabel: categoryLabels[item.category],
  fallbackIcon: categoryIcons[item.category],
}));
---

<section class="site-section gallery-page reveal-section" data-gallery-page>
  <div class="container">
    <div class="gallery-page__head">
      <span class="section-kicker">{galleryText.headKicker}</span>
      <h2 class="section-title">{galleryText.headTitle}</h2>
      <p class="section-copy">
        {galleryText.headCopy}
      </p>
    </div>

    <div class="premium-card gallery-page__filters" aria-label={galleryText.filtersAria}>
      {categories.map((category) => (
        <button
          type="button"
          class:list={['gallery-page__filter', category.id === 'all' && 'is-active']}
          data-gallery-filter={category.id}
          aria-pressed={category.id === 'all' ? 'true' : 'false'}
        >
          {category.label}
        </button>
      ))}
    </div>

    <div class="gallery-page__grid" data-gallery-grid>
      {resolvedGalleryItems.map((item, index) => (
        <button
          type="button"
          class:list={['photo-frame', 'premium-card', 'reveal-section', 'gallery-card', `gallery-card--${item.size}`, !item.imageExists && 'gallery-card--fallback']}
          data-gallery-item
          data-category={item.category}
          data-index={index}
          data-title={item.title}
          data-category-label={item.categoryLabel}
          data-placeholder-label={galleryText.photoComingSoon}
          data-image={item.imageExists ? item.image : ''}
          data-alt={item.alt}
          style={`--reveal-delay: ${Math.min(index, 8) * 45}ms`}
          aria-label={`${galleryText.openPhoto}: ${item.title}`}
        >
          {item.imageExists ? (
            <img src={item.image} alt={item.alt} loading="lazy" decoding="async" />
          ) : (
            <span class="gallery-card__mood" aria-hidden="true">
              <span class="gallery-card__mood-light"></span>
              <span class="gallery-card__mood-shape gallery-card__mood-shape--back"></span>
              <span class="gallery-card__mood-shape gallery-card__mood-shape--front"></span>
              <span class="gallery-card__mood-icon">
                <Icon name={item.fallbackIcon} size={34} strokeWidth={1.75} />
              </span>
            </span>
          )}
          <span class="gallery-card__shade" aria-hidden="true"></span>
          <span class="gallery-card__body">
            <small>{item.categoryLabel}</small>
            <strong>{item.title}</strong>
            {!item.imageExists && (
              <>
                <em>{galleryText.photoComingSoon}</em>
              </>
            )}
          </span>
        </button>
      ))}
    </div>
  </div>

  <div class="gallery-lightbox" data-gallery-lightbox hidden aria-hidden="true">
    <div class="gallery-lightbox__backdrop" data-gallery-close></div>
    <div class="gallery-lightbox__dialog" role="dialog" aria-modal="true" aria-label={galleryText.dialog}>
      <button class="gallery-lightbox__close" type="button" data-gallery-close aria-label={galleryText.close}>
        <Icon name="close" size={22} strokeWidth={2} />
      </button>

      <button class="gallery-lightbox__nav gallery-lightbox__nav--prev" type="button" data-gallery-prev aria-label={galleryText.previous}>
        <Icon name="chevronLeft" size={24} strokeWidth={2} />
      </button>

      <figure class="gallery-lightbox__figure">
        <div class="gallery-lightbox__media" data-gallery-media></div>
        <figcaption>
          <span data-gallery-lightbox-category>Galeria</span>
          <strong data-gallery-lightbox-title>Camping Clepardia</strong>
        </figcaption>
      </figure>

      <button class="gallery-lightbox__nav gallery-lightbox__nav--next" type="button" data-gallery-next aria-label={galleryText.next}>
        <Icon name="chevronRight" size={24} strokeWidth={2} />
      </button>
    </div>
  </div>
</section>

<section class="site-section gallery-final-cta reveal-section">
  <div class="container">
    <div class="premium-card gallery-final-cta__panel">
      <div>
        <span class="section-kicker">{galleryText.ctaKicker}</span>
        <h2>{page.cta.title}</h2>
        <p>
          {page.cta.copy}
        </p>
      </div>
      <div class="gallery-final-cta__actions">
        <a class="btn btn-primary premium-button" href={pricingHref}>{page.cta.primaryLabel}</a>
        <a class="btn btn-glass gallery-final-cta__secondary" href={contactHref}>{page.cta.secondaryLabel}</a>
      </div>
    </div>
  </div>
</section>

<script>
  const galleryRoot = document.querySelector('[data-gallery-page]');

  if (galleryRoot instanceof HTMLElement) {
    const filters = Array.from(galleryRoot.querySelectorAll('[data-gallery-filter]')) as HTMLButtonElement[];
    const grid = galleryRoot.querySelector('[data-gallery-grid]') as HTMLElement | null;
    const items = Array.from(galleryRoot.querySelectorAll('[data-gallery-item]')) as HTMLButtonElement[];
    const lightbox = galleryRoot.querySelector('[data-gallery-lightbox]') as HTMLElement | null;
    const media = galleryRoot.querySelector('[data-gallery-media]') as HTMLElement | null;
    const title = galleryRoot.querySelector('[data-gallery-lightbox-title]') as HTMLElement | null;
    const category = galleryRoot.querySelector('[data-gallery-lightbox-category]') as HTMLElement | null;
    const closeButtons = Array.from(galleryRoot.querySelectorAll('[data-gallery-close]')) as HTMLElement[];
    const prevButton = galleryRoot.querySelector('[data-gallery-prev]') as HTMLButtonElement | null;
    const nextButton = galleryRoot.querySelector('[data-gallery-next]') as HTMLButtonElement | null;
    let activeFilter = 'all';
    let activeIndex = 0;
    let lastFocused: HTMLElement | null = null;
    let previousBodyOverflow = '';

    const getVisibleItems = () => items.filter((item) => !item.hidden);

    const setFilter = (filter: string) => {
      activeFilter = filter;
      filters.forEach((button) => {
        const isActive = button.dataset.galleryFilter === filter;
        button.classList.toggle('is-active', isActive);
        button.setAttribute('aria-pressed', String(isActive));
      });

      grid?.classList.add('is-filtering');
      window.setTimeout(() => {
        items.forEach((item) => {
          const isVisible = filter === 'all' || item.dataset.category === filter;
          item.hidden = !isVisible;
          item.classList.toggle('is-filtered-out', !isVisible);
        });
        window.setTimeout(() => grid?.classList.remove('is-filtering'), 80);
      }, 120);
    };

    const renderLightbox = () => {
      const visibleItems = getVisibleItems();
      const item = visibleItems[activeIndex];
      if (!item || !media) return;

      const image = item.dataset.image ?? '';
      const imageTitle = item.dataset.title ?? 'Camping Clepardia';
      const imageCategory = item.dataset.categoryLabel ?? 'Galeria';
      const imageAlt = item.dataset.alt ?? imageTitle;
      const placeholderLabel = item.dataset.placeholderLabel ?? 'Photo coming soon';

      if (title) title.textContent = imageTitle;
      if (category) category.textContent = imageCategory;

      media.innerHTML = '';
      if (image) {
        const img = document.createElement('img');
        img.src = image;
        img.alt = imageAlt;
        img.decoding = 'async';
        media.append(img);
      } else {
        const fallback = document.createElement('div');
        fallback.className = 'gallery-lightbox__fallback';
        const categoryLabel = document.createElement('span');
        categoryLabel.textContent = imageCategory;
        const heading = document.createElement('strong');
        heading.textContent = imageTitle;
        const note = document.createElement('p');
        note.textContent = placeholderLabel;
        const copy = document.createElement('small');
        copy.textContent = imageAlt;
        fallback.append(categoryLabel, heading, note, copy);
        media.append(fallback);
      }
    };

    const openLightbox = (item: HTMLButtonElement) => {
      const visibleItems = getVisibleItems();
      const nextIndex = visibleItems.indexOf(item);
      if (!lightbox || nextIndex < 0) return;

      lastFocused = document.activeElement instanceof HTMLElement ? document.activeElement : null;
      activeIndex = nextIndex;
      previousBodyOverflow = document.body.style.overflow;
      document.body.style.overflow = 'hidden';
      lightbox.hidden = false;
      lightbox.setAttribute('aria-hidden', 'false');
      lightbox.classList.add('is-open');
      renderLightbox();
      window.setTimeout(() => (galleryRoot.querySelector('[data-gallery-close]') as HTMLButtonElement | null)?.focus(), 40);
    };

    const closeLightbox = () => {
      if (!lightbox || lightbox.hidden) return;
      lightbox.classList.remove('is-open');
      lightbox.setAttribute('aria-hidden', 'true');
      document.body.style.overflow = previousBodyOverflow;
      window.setTimeout(() => {
        lightbox.hidden = true;
        media?.replaceChildren();
        lastFocused?.focus();
      }, 220);
    };

    const moveLightbox = (direction: 1 | -1) => {
      const visibleItems = getVisibleItems();
      if (!visibleItems.length) return;

      activeIndex = (activeIndex + direction + visibleItems.length) % visibleItems.length;
      renderLightbox();
    };

    filters.forEach((button) => {
      button.addEventListener('click', () => setFilter(button.dataset.galleryFilter ?? 'all'));
    });

    items.forEach((item) => {
      item.addEventListener('click', () => openLightbox(item));
    });

    closeButtons.forEach((button) => {
      button.addEventListener('click', closeLightbox);
    });

    prevButton?.addEventListener('click', () => moveLightbox(-1));
    nextButton?.addEventListener('click', () => moveLightbox(1));

    document.addEventListener('keydown', (event) => {
      if (!lightbox || lightbox.hidden) return;
      if (event.key === 'Escape') closeLightbox();
      if (event.key === 'ArrowLeft') moveLightbox(-1);
      if (event.key === 'ArrowRight') moveLightbox(1);
    });
  }
</script>

<style>
.gallery-page {
  background:
    radial-gradient(circle at 88% 10%, rgba(60,179,113,.14), transparent 30%),
    linear-gradient(180deg, #fbfbf7, #f2f7f0);
}

.gallery-page__head {
  max-width: 880px;
  margin-bottom: clamp(28px, 5vw, 50px);
}

.gallery-page__filters {
  display: flex;
  gap: 10px;
  align-items: center;
  margin-bottom: clamp(28px, 5vw, 54px);
  padding: 12px;
  border-radius: 999px;
  overflow-x: auto;
  scrollbar-width: none;
}

.gallery-page__filters::-webkit-scrollbar {
  display: none;
}

.gallery-page__filter {
  flex: 0 0 auto;
  min-height: 44px;
  padding: 10px 18px;
  border-radius: 999px;
  border: 1px solid rgba(22,50,36,.12);
  color: #425448;
  background: rgba(255,255,255,.70);
  font-size: .88rem;
  font-weight: 900;
  transition: transform .22s ease, background .22s ease, border-color .22s ease, color .22s ease, box-shadow .22s ease;
}

.gallery-page__filter:hover,
.gallery-page__filter.is-active {
  color: #fff;
  border-color: rgba(60,179,113,.34);
  background: linear-gradient(135deg, #3CB371, #2f9d63);
  box-shadow: 0 14px 32px rgba(60,179,113,.22);
  transform: translateY(-1px);
}

.gallery-page__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
  transition: opacity .2s ease, transform .2s ease;
}

.gallery-page__grid.is-filtering {
  opacity: .66;
  transform: scale(.995);
}

.gallery-card {
  position: relative;
  min-height: 280px;
  width: 100%;
  display: grid;
  align-items: end;
  padding: 0;
  border-radius: 28px;
  border: 1px solid rgba(255,255,255,.34);
  overflow: hidden;
  color: #fff;
  text-align: left;
  cursor: pointer;
  box-shadow: 0 26px 76px rgba(17, 40, 28, .16);
}

.gallery-card[hidden] {
  display: none;
}

:global(.gallery-card.photo-frame) {
  min-height: 280px;
  border-radius: 28px;
  padding: 0;
}

.gallery-card img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  transform: scale(1.01);
  transition: transform .78s cubic-bezier(.22, 1, .36, 1), filter .78s ease;
}

.gallery-card:hover img {
  transform: scale(1.075);
  filter: saturate(1.08) contrast(1.04);
}

.gallery-card--fallback {
  background:
    linear-gradient(135deg, rgba(14, 44, 29, .20), rgba(14, 42, 29, .64)),
    radial-gradient(circle at 74% 24%, rgba(242, 192, 120, .36), transparent 36%),
    radial-gradient(circle at 16% 78%, rgba(60,179,113,.42), transparent 40%),
    linear-gradient(135deg, #477b59, #173d29 58%, #10291c);
  border-color: rgba(143,224,180,.38);
  box-shadow: 0 30px 88px rgba(17,40,28,.20), 0 0 44px rgba(60,179,113,.16);
}

.gallery-card__mood {
  position: absolute;
  inset: 0;
  max-width: 100%;
  overflow: hidden;
  pointer-events: none;
  contain: paint;
}

.gallery-card__mood::before {
  content: '';
  position: absolute;
  inset: 0;
  opacity: .13;
  background-image:
    radial-gradient(circle at 18% 22%, rgba(255,255,255,.36) 0 1px, transparent 1px 5px),
    radial-gradient(circle at 72% 52%, rgba(255,255,255,.24) 0 1px, transparent 1px 6px);
  background-size: 8px 8px, 12px 12px;
  mix-blend-mode: screen;
}

.gallery-card__mood-light {
  position: absolute;
  right: 14%;
  top: 14%;
  width: clamp(100px, 16vw, 190px);
  aspect-ratio: 1;
  border-radius: 999px;
  background: radial-gradient(circle, rgba(255,236,185,.88), rgba(242,192,120,.42) 42%, transparent 70%);
  opacity: .76;
  filter: blur(.3px);
}

.gallery-card__mood-shape {
  position: absolute;
  left: -10%;
  right: -10%;
  bottom: -17%;
  height: 48%;
  border-radius: 52% 52% 0 0;
  background: linear-gradient(180deg, rgba(60,179,113,.40), rgba(10,30,20,.88));
}

.gallery-card__mood-shape--back {
  bottom: -8%;
  opacity: .54;
  transform: translateX(14%);
}

.gallery-card__mood-shape--front {
  height: 40%;
  background: linear-gradient(180deg, rgba(55,151,96,.70), rgba(10,30,20,.94));
}

.gallery-card__mood-icon {
  position: absolute;
  right: 24px;
  top: 24px;
  width: 68px;
  height: 68px;
  display: grid;
  place-items: center;
  border-radius: 24px;
  color: #f8fff9;
  border: 1px solid rgba(143,224,180,.32);
  background:
    radial-gradient(circle at 28% 18%, rgba(255,255,255,.38), transparent 34%),
    radial-gradient(circle at 72% 78%, rgba(242,192,120,.20), transparent 42%),
    linear-gradient(135deg, rgba(60,179,113,.40), rgba(60,179,113,.18));
  box-shadow: 0 22px 62px rgba(60,179,113,.30), 0 0 36px rgba(60,179,113,.16), inset 0 1px 0 rgba(255,255,255,.20);
}

.gallery-card__mood-icon svg {
  width: 34px;
  height: 34px;
  fill: none;
  stroke: currentColor;
  stroke-width: 1.75;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.gallery-card__shade {
  position: absolute;
  inset: 0;
  background:
    linear-gradient(180deg, transparent 32%, rgba(3,13,9,.74) 100%),
    radial-gradient(circle at 80% 18%, rgba(255,196,121,.18), transparent 34%);
  pointer-events: none;
}

.gallery-card__body {
  position: relative;
  z-index: 2;
  display: grid;
  gap: 8px;
  padding: clamp(22px, 3vw, 30px);
}

.gallery-card__body small {
  color: #8fe0b4;
  font-size: .72rem;
  font-weight: 950;
  letter-spacing: .14em;
  text-transform: uppercase;
}

.gallery-card__body strong {
  color: #fff;
  font-size: clamp(1.25rem, 2.4vw, 1.85rem);
  line-height: 1.08;
  font-weight: 950;
  text-shadow: 0 8px 24px rgba(0,0,0,.32);
}

.gallery-card__body em,
.gallery-card__body p {
  max-width: 440px;
  color: rgba(255,255,255,.78);
  font-style: normal;
  line-height: 1.5;
}

.gallery-card__body em {
  width: fit-content;
  padding: 6px 10px;
  border: 1px solid rgba(143,224,180,.24);
  border-radius: 999px;
  color: #e9fff1;
  background: rgba(60,179,113,.13);
  font-size: .68rem;
  font-weight: 900;
  letter-spacing: .10em;
  text-transform: uppercase;
}

.gallery-card__body p {
  margin: 0;
  font-size: .92rem;
}

.gallery-lightbox {
  position: fixed;
  inset: 0;
  z-index: 2600;
  display: grid;
  place-items: center;
  padding: clamp(18px, 4vw, 42px);
  opacity: 0;
  visibility: hidden;
  transition: opacity .26s ease, visibility .26s ease;
}

.gallery-lightbox.is-open {
  opacity: 1;
  visibility: visible;
}

.gallery-lightbox__backdrop {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 72% 18%, rgba(60,179,113,.16), transparent 34%),
    rgba(4, 11, 8, .88);
  backdrop-filter: blur(18px);
}

.gallery-lightbox__dialog {
  position: relative;
  z-index: 1;
  width: min(1120px, 100%);
  display: grid;
  grid-template-columns: auto minmax(0, 1fr) auto;
  gap: clamp(10px, 2vw, 18px);
  align-items: center;
}

.gallery-lightbox__figure {
  margin: 0;
  overflow: hidden;
  border-radius: 28px;
  border: 1px solid rgba(255,255,255,.14);
  background: rgba(255,255,255,.08);
  box-shadow: 0 34px 100px rgba(0,0,0,.42);
}

.gallery-lightbox__media {
  width: 100%;
  min-height: min(70vh, 680px);
  display: grid;
  place-items: center;
  background:
    linear-gradient(135deg, rgba(7,22,15,.22), rgba(7,22,15,.72)),
    radial-gradient(circle at 72% 20%, rgba(232,158,84,.22), transparent 36%),
    linear-gradient(135deg, #25412f, #0c1912);
}

.gallery-lightbox__media img {
  width: 100%;
  height: 100%;
  max-height: 70vh;
  display: block;
  object-fit: contain;
}

.gallery-lightbox__fallback {
  min-height: min(70vh, 680px);
  width: 100%;
  display: grid;
  place-items: center;
  gap: 12px;
  align-content: center;
  padding: 34px;
  color: #fff;
  text-align: center;
}

.gallery-lightbox__fallback span {
  color: #8fe0b4;
  font-size: .78rem;
  font-weight: 950;
  letter-spacing: .14em;
  text-transform: uppercase;
}

.gallery-lightbox__fallback strong {
  max-width: 680px;
  font-size: clamp(2rem, 6vw, 4.4rem);
  line-height: .98;
  font-weight: 950;
}

.gallery-lightbox__fallback p {
  margin: 0;
  padding: 8px 13px;
  border: 1px solid rgba(143,224,180,.24);
  border-radius: 999px;
  color: #d9ffe9;
  background: rgba(60,179,113,.14);
  font-size: .86rem;
  font-weight: 900;
}

.gallery-lightbox__fallback small {
  max-width: 620px;
  color: rgba(255,255,255,.72);
  font-size: 1rem;
  line-height: 1.6;
}

.gallery-lightbox figcaption {
  display: grid;
  gap: 8px;
  padding: 20px clamp(20px, 3vw, 30px);
  color: #fff;
  background: rgba(8,18,14,.88);
}

.gallery-lightbox figcaption span {
  color: #8fe0b4;
  font-size: .74rem;
  font-weight: 950;
  letter-spacing: .14em;
  text-transform: uppercase;
}

.gallery-lightbox figcaption strong {
  font-size: clamp(1.25rem, 2vw, 1.65rem);
  line-height: 1.16;
  font-weight: 950;
}

.gallery-lightbox__close,
.gallery-lightbox__nav {
  width: 52px;
  height: 52px;
  display: grid;
  place-items: center;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,.14);
  color: #fff;
  background: rgba(8,18,14,.78);
  box-shadow: 0 20px 54px rgba(0,0,0,.30);
  backdrop-filter: blur(16px);
}

.gallery-lightbox__close {
  position: absolute;
  right: 14px;
  top: 14px;
  z-index: 3;
}

.gallery-lightbox__close svg,
.gallery-lightbox__nav svg {
  width: 24px;
  height: 24px;
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.gallery-lightbox__close:hover,
.gallery-lightbox__nav:hover {
  border-color: rgba(143,224,180,.42);
  box-shadow: 0 24px 64px rgba(0,0,0,.34), 0 0 28px rgba(60,179,113,.22);
  transform: translateY(-1px);
}

.gallery-final-cta {
  padding-top: 0;
  background:
    radial-gradient(circle at 18% 20%, rgba(60,179,113,.18), transparent 34%),
    linear-gradient(180deg, #f2f7f0, #101b14);
}

.gallery-final-cta__panel {
  display: grid;
  gap: 28px;
  padding: clamp(28px, 5vw, 54px);
  border-radius: 28px;
  color: #fff;
  background:
    linear-gradient(135deg, rgba(10,28,18,.92), rgba(17,39,27,.82)),
    radial-gradient(circle at 86% 18%, rgba(239,174,99,.18), transparent 36%);
  border-color: rgba(255,255,255,.14);
}

.gallery-final-cta__panel .section-kicker {
  color: #8fe0b4;
}

.gallery-final-cta__panel h2 {
  max-width: 760px;
  margin: 10px 0 16px;
  color: #fff;
  font-size: clamp(2rem, 5vw, 4rem);
  line-height: .98;
  font-weight: 950;
}

.gallery-final-cta__panel p {
  max-width: 720px;
  margin: 0;
  color: rgba(255,255,255,.76);
  line-height: 1.75;
}

.gallery-final-cta__actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}

.gallery-final-cta__secondary {
  display: inline-flex;
  min-height: 50px;
  align-items: center;
  justify-content: center;
  padding: 14px 24px;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,.18);
  color: #fff;
  background: rgba(255,255,255,.08);
  font-weight: 900;
  letter-spacing: .06em;
}

.gallery-final-cta__secondary:hover {
  color: #fff;
  border-color: rgba(143,224,180,.42);
  background: rgba(255,255,255,.14);
}

@media (min-width: 720px) {
  .gallery-page__grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    grid-auto-flow: dense;
  }

  .gallery-card--wide,
  .gallery-card--feature {
    grid-column: span 2;
  }

  .gallery-card--tall {
    min-height: 420px;
  }
}

@media (min-width: 1080px) {
  .gallery-page__grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .gallery-card--feature {
    grid-column: span 2;
    grid-row: span 2;
    min-height: 560px;
  }

  .gallery-card--wide {
    grid-column: span 2;
  }

  .gallery-final-cta__panel {
    grid-template-columns: 1fr auto;
    align-items: end;
  }
}

@media (max-width: 680px) {
  .gallery-page__filters {
    margin-left: calc(var(--container-padding-mobile) * -1);
    margin-right: calc(var(--container-padding-mobile) * -1);
    padding-left: var(--container-padding-mobile);
    padding-right: var(--container-padding-mobile);
    border-radius: 0;
    border-left: 0;
    border-right: 0;
  }

  .gallery-card {
    min-height: 310px;
  }

  .gallery-lightbox {
    padding: 12px;
  }

  .gallery-lightbox__dialog {
    grid-template-columns: 1fr;
    width: 100%;
    max-height: calc(100dvh - 24px);
  }

  .gallery-lightbox__figure {
    border-radius: 24px;
  }

  .gallery-lightbox__media,
  .gallery-lightbox__fallback {
    min-height: 56vh;
    max-height: 80dvh;
  }

  .gallery-lightbox__media img {
    width: auto;
    height: auto;
    max-width: calc(100vw - 24px);
    max-height: 80dvh;
  }

  .gallery-lightbox__nav {
    position: absolute;
    z-index: 3;
    top: 50%;
    transform: translateY(-50%);
  }

  .gallery-lightbox__nav--prev {
    left: 10px;
  }

  .gallery-lightbox__nav--next {
    right: 10px;
  }

  .gallery-final-cta__actions {
    display: grid;
  }

  .gallery-final-cta__actions a {
    width: 100%;
  }
}

@media (prefers-reduced-motion: reduce) {
  .gallery-page *,
  .gallery-lightbox *,
  .gallery-final-cta * {
    transition-duration: .01ms !important;
    animation: none !important;
  }
}
</style>
